const Observation = () => <div className="observation">
    <h1>observações</h1>
    <textarea></textarea>
    <div className="obs-buttons">
        <button onClick={() => null}>Fechar</button>
        <button onClick={() => null}>Salvar</button>
    </div>
</div>;

export default Observation;